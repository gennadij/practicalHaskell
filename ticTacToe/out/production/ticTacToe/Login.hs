module Login where

-- import Control.Monad.Trans.State
-- import Control.Monad.IO.Class (liftIO)

-- data WebsiteResult = OK | Err String
-- data WebsiteState = LoggedIn | LoggedOut

-- logIn :: String -> State WebsiteState WebsiteResult
-- logIn pwd = do
  -- if pwd == "pwd"
    -- then put LoggedIn
    -- else put LoggedOut
  -- checkOk
    -- where
      -- checkOk :: State WebsiteState WebsiteResult
      -- checkOk = do
        -- state <- get
        -- if state == LoggedIn
          -- then return OK
          -- else return $ Error "Bad Login."

